
spring:
  application:
    name: app-order-server
  profiles: 
    active: local
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        service-id: erp-config
#      fail-fast: true
#      username: root
#      password: admin123!
#      profile: jzyc
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      percentage: 1.0
#新版配置，否则后面dashboard无法找到hystrix.stream     
management:
  endpoints:
    web:
      exposure:
        include: '*'